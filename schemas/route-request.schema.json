{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/harzva/codexhome-manager/schemas/route-request.schema.json",
  "title": "CodexHome Route Request",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "requestId",
    "taskKind",
    "estimatedContextTokens",
    "estimatedOutputTokens"
  ],
  "properties": {
    "schemaVersion": { "const": "codexhome.route-request.v1" },
    "requestId": { "$ref": "#/$defs/routingIdentifier" },
    "taskKind": { "$ref": "#/$defs/taskKind" },
    "estimatedContextTokens": { "type": "integer", "minimum": 1 },
    "estimatedOutputTokens": { "type": "integer", "minimum": 0 },
    "requiredCapabilities": {
      "type": "array",
      "maxItems": 64,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/routingIdentifier" }
    },
    "preferredSpecialties": {
      "type": "array",
      "maxItems": 64,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/routingIdentifier" }
    },
    "sensitiveDirectory": { "type": "boolean", "default": false },
    "requiredSecurityDomain": { "$ref": "#/$defs/nullableRoutingIdentifier" },
    "lockedHome": { "$ref": "#/$defs/nullableRoutingIdentifier" },
    "lockedModel": { "$ref": "#/$defs/nullableRoutingIdentifier" },
    "maxEstimatedCostMicrousd": { "type": ["integer", "null"], "minimum": 1 },
    "allowDegraded": { "type": "boolean", "default": false }
  },
  "allOf": [
    {
      "if": {
        "required": ["sensitiveDirectory"],
        "properties": { "sensitiveDirectory": { "const": true } }
      },
      "then": {
        "required": ["lockedHome"],
        "properties": { "lockedHome": { "type": "string", "minLength": 1 } }
      }
    }
  ],
  "$defs": {
    "routingIdentifier": {
      "type": "string",
      "minLength": 1,
      "maxLength": 160,
      "pattern": "^(?![/~])(?![A-Za-z]:/)(?!.*\\.\\.)(?!.*//)[A-Za-z0-9._:@/-]+$"
    },
    "nullableRoutingIdentifier": {
      "oneOf": [
        { "$ref": "#/$defs/routingIdentifier" },
        { "type": "null" }
      ]
    },
    "taskKind": {
      "enum": [
        "simple_code",
        "architecture",
        "long_document",
        "batch",
        "retrieval",
        "visual",
        "general"
      ]
    }
  }
}
